<form method="post" action="{{ path('app_issue_book_delete', {'id': issue_book.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ issue_book.id) }}">
    <div class="container container-table">
        <button class="btn btn-danger">Delete</button>
    </div>
</form>
